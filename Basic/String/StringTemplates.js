//Back-Tics Syntax.
// Template String use back-ticks(``) rather than the quaotes
// ("") to define a string . 
let text = `Hello world`;
console.log(text);

// Quotes Inside Strings . 
// Template Strings allow both single and double quotes inside a strings . 

let text2 = `He's often called "Johnny"`;
console.log(text2);